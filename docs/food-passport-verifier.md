# Food passport receipt verifier

Receipt upload was removed from the public Food Passport.

It never verified a merchant, amount, or visit. The file stayed on-device and only marked a place tried, which the Mark tried button already does.

Do not show a receipt-check UI until there is a real verification product.
